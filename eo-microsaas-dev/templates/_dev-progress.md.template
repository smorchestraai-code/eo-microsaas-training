# Dev Progress — {PROJECT_NAME}

> Tracker for `/eo-guide`. Filesystem is the source of truth; this file is a view.
> Every numbered command (`/1-eo-dev-start` through `/8-eo-retro`) writes here at end-of-run. `/eo-guide` reconciles drift.

Last updated: {DATE}
Current sprint: {SPRINT_NUMBER}
Last command: none

## Stories

| # | Story | Phase | Status | Plan | Tests | Score | Shipped | Notes |
|---|-------|-------|--------|------|-------|-------|---------|-------|
| 1 | {STORY_1_TITLE} | MVP | ⬜ not started | — | — | — | — | — |
| 2 | {STORY_2_TITLE} | MVP | ⬜ not started | — | — | — | — | — |
| 3 | {STORY_3_TITLE} | MVP | ⬜ not started | — | — | — | — | — |
| 4 | {STORY_4_TITLE} | MVP | ⬜ not started | — | — | — | — | — |
| 5 | {STORY_5_TITLE} | v2 | 🧊 frozen | — | — | — | — | Deferred — tagged [@Phase2] |
| 6 | {STORY_6_TITLE} | v2 | 🧊 frozen | — | — | — | — | Deferred — tagged [@Phase2] |
| 7 | {STORY_7_TITLE} | v2 | 🧊 frozen | — | — | — | — | Deferred — tagged [@Phase2] |

## Status legend

- ⬜ not started — no plan file yet
- 📝 planned — `docs/plans/story-N-*.md` exists, tests still `.skip`
- 🔨 coding — tests being written/passed
- 🧪 scoring — tests pass, awaiting `/5-eo-score`
- 🩹 bridging gaps — score 80–89, `/6-eo-bridge-gaps` running
- 🧊 frozen — deferred to v2 (tagged `[@Phase2]` in BRD). Skipped in Weekend MVP.
- ✅ shipped — merged to main + deployed
- ⚠️ inconsistent — state checks failed, see `/eo-guide` diagnostic

## Invariants

- Score < 80 → status cannot be `shipped`
- `shipped` → must have both score ≥ 90 row AND deploy log entry
- `coding` without a plan file = impossible; `/eo-guide` flags as inconsistent
- Phase `v2` rows stay `🧊 frozen` until MVP is shipped. Unfreeze by editing the BRD `[@Phase2]` tag + running `/eo-dev-repair`.

## Cadence — weekend shipment

MVP = one weekend. Not 5 weeks.

| Day | Block | What ships |
|-----|-------|-----------|
| **Fri evening** | `/1-eo-dev-start` | Mode picked (M0–M3), scaffold landed, BRD carved into Weekend MVP + v2, tracker seeded |
| **Saturday** | `/2-eo-dev-plan` → `/3-eo-code` | Stories 1–4 one at a time, TDD, elegance pause per commit |
| **Sunday** | `/4-eo-review` → `/5-eo-score` → `/6-eo-bridge-gaps` → `/7-eo-ship` | Self-scored; 90+ ships to prod, deploy + health check, first 3 users told |

After weekend — `/8-eo-retro` once, then v2 planning starts by flipping a `🧊 frozen` row to `⬜ not started` + running `/2-eo-dev-plan story-5`.
